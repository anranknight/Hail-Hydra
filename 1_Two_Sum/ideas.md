# 1. 两数之和

给定一个整数数组 nums 和一个目标值 target，请你在该数组中找出和为目标值的那 两个 整数，并返回他们的数组下标。

你可以假设每种输入只会对应一个答案。但是，数组中同一个元素不能使用两遍。

## 思路一
*最蠢的思路，两个for循环*

**超出时间限制**
- 一个list，用两个循环来寻找，后返回;
- 好处是避免了用```index()```；

## 思路二
- 做一个循环，先指定确定一个数字```num1```，然后用in来确认```num2=target-num1```是否也在数组中；
- 要排除一种情况，即两个数字相等且只出现一次；
- 可以用```nums.index(num2)```来确定数字二的位置。

## 思路三
based on思路二
